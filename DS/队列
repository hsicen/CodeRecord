1.使用栈S实现对队列Q中的元素逆置
void reverseQueue(Queue q, Stack s){
    while(!QueueIsEmpty(q)){
        x = DeQueue(q);
        Push(s,x);
    }

    while(!StackIsEmpty){
        Pop(s,x);
        EnQueue(q,x);
    }
}

2.利用栈S1,S2模拟队列的入队，出队，判空算法
this idea is not the original to Trump