优化一：使用include来复用布局,减少重复布局的创建
	id，layout和宽高属性
	在另一个布局里面,include了别的布局，在寻找这个布局下面的的控件时，注意要在找到这个布局，然后再用这个布局来寻找

优化二：ViewStub   通过调用setVisibility函数或者Inflate函数来达到延迟加载的效果

优化三：使用Merge布局来减少include布局文件时的层级
	要复用的布局的跟标签使用Merge标签来代替